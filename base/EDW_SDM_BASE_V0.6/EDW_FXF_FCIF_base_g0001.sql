-- SQL for table: EDW_FXF_FCIF
/* TRANSFORM_DATA */

-- 異動方式待補/待調整，以下目前為公版
/* 1. 刪除Base DATA_EXCH_DATE='#資料交換期別#' 之資料 */
DELETE FROM ${TARGET_SCHEMA}.${TARGET_TABLE} WHERE DATA_EXCH_DATE = TO_DATE('${BD_EXCH_DATE}', 'yyyy-MM-dd');

-- Trinity SQL Script
/* 2. 抽取Stage DATA_EXCH_DATE = '#資料交換期別#'之資料進行新增 */
INSERT INTO ${TARGET_SCHEMA}.${TARGET_TABLE}
(
CIFKEY,
CORPNO,
ENAME1,
ENAME2,
ENAME3,
EADDR1,
EADDR2,
EADDR3,
EADDR4,
EADDR5,
BCODE,
QID,
CUTYPE,
CUDTL,
BSCD,
PIDNO,
PNAME,
BNAME,
BIRDT,
ISUDT,
EXPDT,
CTYPE,
AREANO,
TELNO,
EXTNO,
FAXAREA,
FAXNO,
EMAIL,
CONAL,
RGNAL,
DERNAL,
CUSTCD,
MBRNO,
IBRNO,
MGP,
ADVCD,
KINBR,
TLRNO,
ZIP,
SCOP,
SENDFG,
CADDR,
CADDR2,
CADDR3,
CADDR4,
CADDR5,
EBFG,
VACTCD,
RATINGORG,
RATING,
OBUCIFKEY,
GTCUTYP,
CREDATE,
OBUCRE,
ICIFKEY,
BKIND2,
LEI,
ROMANAME,
DATA_UPDATE_TIME,
DATA_EXCH_DATE
)
SELECT
CIFKEY,
CORPNO,
ENAME1,
ENAME2,
ENAME3,
EADDR1,
EADDR2,
EADDR3,
EADDR4,
EADDR5,
BCODE,
QID,
CUTYPE,
CUDTL,
BSCD,
PIDNO,
PNAME,
BNAME,
SDG.UFN_NORM_DATE(CASE WHEN NVL(TRIM(BIRDT), '')='' THEN NULL WHEN SDG.UFN_ADDATECHECK_DATE(TRIM(BIRDT))='0' THEN NULL ELSE BIRDT END) AS BIRDT,
SDG.UFN_NORM_DATE(CASE WHEN NVL(TRIM(ISUDT), '')='' THEN NULL WHEN SDG.UFN_ADDATECHECK_DATE(TRIM(ISUDT))='0' THEN NULL ELSE ISUDT END) AS ISUDT,
SDG.UFN_NORM_DATE(CASE WHEN NVL(TRIM(EXPDT), '')='' THEN NULL WHEN SDG.UFN_ADDATECHECK_DATE(TRIM(EXPDT))='0' THEN NULL ELSE EXPDT END) AS EXPDT,
CTYPE,
AREANO,
TELNO,
EXTNO,
FAXAREA,
FAXNO,
EMAIL,
CONAL,
RGNAL,
DERNAL,
CUSTCD,
MBRNO,
IBRNO,
MGP,
ADVCD,
KINBR,
TLRNO,
ZIP,
SCOP,
SENDFG,
CADDR,
CADDR2,
CADDR3,
CADDR4,
CADDR5,
EBFG,
VACTCD,
RATINGORG,
RATING,
OBUCIFKEY,
GTCUTYP,
SDG.UFN_NORM_DATE(CASE WHEN NVL(TRIM(CREDATE), '')='' THEN NULL WHEN SDG.UFN_ADDATECHECK_DATE(TRIM(CREDATE))='0' THEN NULL ELSE CREDATE END) AS CREDATE,
SDG.UFN_NORM_DATE(CASE WHEN NVL(TRIM(OBUCRE), '')='' THEN NULL WHEN SDG.UFN_ADDATECHECK_DATE(TRIM(OBUCRE))='0' THEN NULL ELSE OBUCRE END) AS OBUCRE,
ICIFKEY,
BKIND2,
LEI,
ROMANAME,
CURRENT_TIMESTAMP,
to_date('${BD_EXCH_DATE}', 'yyyy-MM-dd')
FROM ${SOURCE_TABLE_OWNER}.${SOURCE_TABLE}
WHERE DATA_EXCH_DATE = to_date('${BD_EXCH_DATE}', 'yyyy-MM-dd')
;

.export @LOAD_ROWCNT updatecnt;
