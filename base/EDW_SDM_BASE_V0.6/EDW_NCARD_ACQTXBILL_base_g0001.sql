-- SQL for table: EDW_NCARD_ACQTXBILL
/* TRANSFORM_DATA */

-- 異動方式待補/待調整，以下目前為公版
/* 1. 刪除Base DATA_EXCH_DATE='#資料交換期別#' 之資料 */
DELETE FROM ${TARGET_SCHEMA}.${TARGET_TABLE} WHERE DATA_EXCH_DATE = TO_DATE('${BD_EXCH_DATE}', 'yyyy-MM-dd');

-- Trinity SQL Script
/* 2. 抽取Stage DATA_EXCH_DATE = '#資料交換期別#'之資料進行新增 */
INSERT INTO ${TARGET_SCHEMA}.${TARGET_TABLE}
(
ACQ_BU,
MER_NBR,
POS_NBR,
SETTLE_DTE,
POSTING_DTE,
STMT_DTE,
FILE_BATCH_NO,
PMT_DTE_EST,
PMT_DTE,
PMT_SEQ,
SEQ,
CARD_TYPE,
ONUS_CARD_FLAG,
DOMESTIC_CARD_FLAG,
MT_TYPE,
CODE,
SOURCE_CODE,
CARD_NBR,
DESCR,
MEMO,
STMT_INVISIBLE,
PROCESS_CNT,
PROCESS_AMT,
PROGRAM,
USER_CHAR_1,
USER_CHAR_2,
USER_CHAR_3,
USER_CHAR_4,
USER_CHAR_5,
USER_AMT_1,
USER_AMT_2,
USER_AMT_3,
USER_AMT_4,
USER_AMT_5,
USER_DATE_1,
USER_DATE_2,
USER_DATE_3,
USER_DATE_4,
USER_DATE_5,
POST_RESULT,
MNT_DT,
MNT_USER,
DATA_UPDATE_TIME,
DATA_EXCH_DATE
)
SELECT
ACQ_BU,
MER_NBR,
POS_NBR,
SDG.UFN_NORM_DATE(CASE WHEN NVL(TRIM(SETTLE_DTE), '')='' THEN NULL WHEN SDG.UFN_ADDATECHECK_DATE(TRIM(SETTLE_DTE))='0' THEN NULL ELSE SETTLE_DTE END) AS SETTLE_DTE,
SDG.UFN_NORM_DATE(CASE WHEN NVL(TRIM(POSTING_DTE), '')='' THEN NULL WHEN SDG.UFN_ADDATECHECK_DATE(TRIM(POSTING_DTE))='0' THEN NULL ELSE POSTING_DTE END) AS POSTING_DTE,
SDG.UFN_NORM_DATE(CASE WHEN NVL(TRIM(STMT_DTE), '')='' THEN NULL WHEN SDG.UFN_ADDATECHECK_DATE(TRIM(STMT_DTE))='0' THEN NULL ELSE STMT_DTE END) AS STMT_DTE,
FILE_BATCH_NO,
SDG.UFN_NORM_DATE(CASE WHEN NVL(TRIM(PMT_DTE_EST), '')='' THEN NULL WHEN SDG.UFN_ADDATECHECK_DATE(TRIM(PMT_DTE_EST))='0' THEN NULL ELSE PMT_DTE_EST END) AS PMT_DTE_EST,
SDG.UFN_NORM_DATE(CASE WHEN NVL(TRIM(PMT_DTE), '')='' THEN NULL WHEN SDG.UFN_ADDATECHECK_DATE(TRIM(PMT_DTE))='0' THEN NULL ELSE PMT_DTE END) AS PMT_DTE,
PMT_SEQ,
SEQ,
CARD_TYPE,
ONUS_CARD_FLAG,
DOMESTIC_CARD_FLAG,
MT_TYPE,
CODE,
SOURCE_CODE,
CARD_NBR,
DESCR,
MEMO,
STMT_INVISIBLE,
PROCESS_CNT,
PROCESS_AMT,
PROGRAM,
USER_CHAR_1,
USER_CHAR_2,
USER_CHAR_3,
USER_CHAR_4,
USER_CHAR_5,
USER_AMT_1,
USER_AMT_2,
USER_AMT_3,
USER_AMT_4,
USER_AMT_5,
SDG.UFN_NORM_DATE(CASE WHEN NVL(TRIM(USER_DATE_1), '')='' THEN NULL WHEN SDG.UFN_ADDATECHECK_DATE(TRIM(USER_DATE_1))='0' THEN NULL ELSE USER_DATE_1 END) AS USER_DATE_1,
SDG.UFN_NORM_DATE(CASE WHEN NVL(TRIM(USER_DATE_2), '')='' THEN NULL WHEN SDG.UFN_ADDATECHECK_DATE(TRIM(USER_DATE_2))='0' THEN NULL ELSE USER_DATE_2 END) AS USER_DATE_2,
SDG.UFN_NORM_DATE(CASE WHEN NVL(TRIM(USER_DATE_3), '')='' THEN NULL WHEN SDG.UFN_ADDATECHECK_DATE(TRIM(USER_DATE_3))='0' THEN NULL ELSE USER_DATE_3 END) AS USER_DATE_3,
SDG.UFN_NORM_DATE(CASE WHEN NVL(TRIM(USER_DATE_4), '')='' THEN NULL WHEN SDG.UFN_ADDATECHECK_DATE(TRIM(USER_DATE_4))='0' THEN NULL ELSE USER_DATE_4 END) AS USER_DATE_4,
SDG.UFN_NORM_DATE(CASE WHEN NVL(TRIM(USER_DATE_5), '')='' THEN NULL WHEN SDG.UFN_ADDATECHECK_DATE(TRIM(USER_DATE_5))='0' THEN NULL ELSE USER_DATE_5 END) AS USER_DATE_5,
POST_RESULT,
SDG.UFN_NORM_DATE(CASE WHEN NVL(TRIM(MNT_DT), '')='' THEN NULL WHEN SDG.UFN_ADDATECHECK_DATE(TRIM(MNT_DT))='0' THEN NULL ELSE MNT_DT END) AS MNT_DT,
MNT_USER,
CURRENT_TIMESTAMP,
to_date('${BD_EXCH_DATE}', 'yyyy-MM-dd')
FROM ${SOURCE_TABLE_OWNER}.${SOURCE_TABLE}
WHERE DATA_EXCH_DATE = to_date('${BD_EXCH_DATE}', 'yyyy-MM-dd')
;

.export @LOAD_ROWCNT updatecnt;
